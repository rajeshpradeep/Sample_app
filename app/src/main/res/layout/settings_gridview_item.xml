<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="175dp"
    android:layout_height="175dp"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:id="@+id/settings_item_clay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:focusableInTouchMode="false"
        android:minHeight="80dp"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="12dp"
        app:cardElevation="3dp"
        app:cardMaxElevation="3dp"
        app:cardPreventCornerOverlap="false"
        app:cardUseCompatPadding="true"
        app:contentPadding="0dp">

        <LinearLayout
            android:id="@+id/settings_item_llay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:background="@null"
            android:foreground="?android:attr/selectableItemBackground"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingTop="@dimen/common_margin_5"
            android:paddingBottom="@dimen/common_margin_10">


            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/settings_item_img"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:adjustViewBounds="true"
                android:button="@android:color/transparent"
                android:clickable="false"
                android:contentDescription="@string/img_desc"
                android:focusable="false"
                app:srcCompat="@drawable/ic_user_green"
                android:scaleType="centerInside" />

            <TextView
                android:id="@+id/settings_item_tview"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:maxLines="2"
                android:padding="@dimen/common_margin_10"
                android:text="@string/adapter_management"
                android:textColor="@color/hint_color"
                android:textSize="@dimen/text_14" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>